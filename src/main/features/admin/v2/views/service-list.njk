{% extends "layout.njk" %}

  {% set title = t('Services | ' + serviceName) %}
  {% set heading = t('Services') %}

  {% block content %}

<div class="column-full">
  <table>
    <thead>
    <tr>
      <th scope="col">Name</th>
    </tr>
    </thead>
    <tbody>
    {% for service in services %}
    <tr>
      <td>{{ service.name }}</td>
    </tr>
    {% endfor %}
    </tbody>
  </table>

  <p><a href="/admin/welcome/" class="link-back">{{ t("Back to welcome page") }}</a></p>
</div>

  {% endblock %}
