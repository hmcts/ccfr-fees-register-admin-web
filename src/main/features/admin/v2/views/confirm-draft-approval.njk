{% from "includes/macros/form.njk" import errorSummary, textArea, textInput, radioGroup, submitButton, selectInline, selectReferenceData %}
  {% extends "layout.njk" %}

  {% set title = t('Fee details') %}
  {% block content %}
    <div class="grid-row">
      <div class="column-full padding-left-right-0">
        <h1 class="heading-xlarge approval-confirm-h1">
          {{t('Are you sure you want to submit this fee for approval?')}}
        </h1>
      </div>
      <div class="column-full approval-confirm-link padding-left-right-0">
        <a href="/admin/v2/draft-fees">{{ t('Don’t submit this fee for approval') }}</a>
      </div>
      <div class="column-full padding-left-right-0">

      <form method="post" action="">
       {{ csrfProtection(csrfToken) }}
        <input type="hidden" name="feeCode" value="{{ feeCode }}"/>
        <input type="hidden" name="version" value="{{ version }}"/>
        <input type="hidden" name="action" value="submit"/>
        <input type="submit" class="button custom-secondary-btn" value="Request approval">
      </form>
      </div>
    </div>
  {% endblock %}
