{% extends "layout.njk" %}

  {% set title = t('channels | ' + serviceName) %}
  {% set heading = t('channels') %}

  {% block content %}

<div class="column-full">
  <table>
    <thead>
    <tr>
      <th scope="col">Name</th>
    </tr>
    </thead>
    <tbody>
    {% for channel in channels %}
    <tr>
      <td>{{ channel.name }}</td>
    </tr>
    {% endfor %}
    </tbody>
  </table>

  <p><a href="/admin/welcome/" class="link-back">{{ t("Back to welcome page") }}</a></p>
</div>

  {% endblock %}
