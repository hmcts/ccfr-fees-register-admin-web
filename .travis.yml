language: node_js
node_js:
  - "node"
cache: yarn
script:
  - npm install -g codecov
  - yarn --version
  - yarn check --integrity
  - yarn setup
  - yarn lint
  - yarn test
  - yarn test:routes
  - yarn test:a11y
  - yarn test:coverage-unit
after_success:
  - yarn test:codacy-upload
  - codecov
